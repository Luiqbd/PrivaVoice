plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.privavoice.privavoice"
    compileSdk = 36
    ndkVersion = "26.1.10909125"

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.privavoice.privavoice"
        minSdk = 24
        targetSdk = 36
        versionCode = 1
        versionName = "1.0.0"

        // Enable multidex for larger apps
        multiDexEnabled true
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
            // Disable minify and shrink to include native libs
            minifyEnabled = false
            shrinkResources = false
        }
    }

    // Ensure native libs are packaged
    packagingOptions {
        jniLibs {
            useLegacyPackaging = true
        }
    }

    // Native build configuration for Whisper and Llama
    externalNativeBuild {
        cmake {
            path "src/main/cpp/CMakeLists.txt"
            version "3.18.1"
        }
    }

    buildFeatures {
        prefab true
    }
    
    sourceSets {
        main {
            // CRITICAL: Include native libs from jniLibs
            jniLibs.srcDirs = ['src/main/jniLibs']
            // Also include assets explicitly
            assets.srcDirs = ['../../assets/models']
        }
    }
}

// IMPORTANT: Keep flutter source pointing to project root
flutter {
    source = "../.."
}

// Add dependencies for prefab (if available)
dependencies {
    // AAR dependencies can be added here if needed
}
